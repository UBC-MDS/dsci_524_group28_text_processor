"""
A module that reads a text file, removes all instances of a string,
and then writes the output to a new file.
"""

def text_remove(input_path: str, output_path: str, string_to_remove: str):
    """
    Removes all instances of a string in a text file, then writes the result into another file.

    Parameters
    ----------
    input_path : str
        The file path to the text file to read.
    output_path : str
        The file path to write the updated text to.
    string_to_remove : str
        The string to remove.

    Returns
    -------
    None

    Examples
    --------
    >>> text_remove("example.txt", "removed.txt", "foo")
    """
    pass
